# Contexte : cris-et-ronronnements

## 🔧 Commandes utiles (PlanetHoster)

| Action | Commande |
|--------|----------|
| Activer Node.js 20.x | `source /home/crisfahs/nodevenv/nocstorage/20/bin/activate` |
| Installer les dépendances | `/home/crisfahs/nodevenv/nocstorage/20/bin/npm install --production` |
| Démarrer avec PM2 | `/home/crisfahs/nodevenv/nocstorage/20/lib/node_modules/.bin/pm2 start app.js --name "cris-et-ronronnements"` |
| Redémarrer Passenger | `touch /home/crisfahs/nodevenv/nocstorage/tmp/restart.txt` |
| Voir les logs | `tail -f /var/log/passenger.log` |

---

## ⚠️ Problèmes courants et solutions

| Problème | Cause | Solution |
|----------|-------|----------|
| **"It works!" s'affiche** | Passenger charge une app par défaut | Corriger `.htaccess` + supprimer `public_html/index.html` |
| `npm command not found` | Node.js/npm non activé | `source /home/.../20/bin/activate` |
| `pm2 command not found` | PM2 non installé globalement | Utiliser le chemin absolu `/home/.../node_modules/.bin/pm2` |

---

## 🔄 Pour les prochaines mises à jour
1. **Toujours vérifier** :
   - `.htaccess` dans `/public_html/`.
   - `PassengerAppRoot` pointe vers `/home/crisfahs/nodevenv/nocstorage`.
   - `app.js` écoute sur `process.env.PORT`.

2. **Ne pas oublier** :
   - Activer Node.js 20.x avec `source /home/.../20/bin/activate`.
   - Utiliser les chemins absolus pour npm/pm2.

3. **En cas de problème** :
   - Vérifier les logs : `tail -f /var/log/passenger.log`.
   - Note : le support PlanetHoster ne donne pas de conseils de développement.